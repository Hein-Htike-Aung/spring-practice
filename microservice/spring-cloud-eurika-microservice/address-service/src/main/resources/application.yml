spring:
  application:
    name: address-service
  jpa:
    defer-datasource-initialization: true


server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8083/eureka